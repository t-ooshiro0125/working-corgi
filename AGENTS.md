# Working Corgi エージェントガイド

## 対話と変更

### 応答

- 原則: 日本語で応答する。
- 例外: ユーザーが別の言語を指定した場合は、その言語で応答する。

### 作業モード

- 原則: レビュー・助言モードとし、ファイル、Git 状態、外部サービスを変更しない。
- 例外: ユーザーが明示的に変更を依頼した場合は、その依頼範囲に限って変更する。

### 確認と説明

- 事前確認: コードに依存する質問へ回答する前に、最新の関連ファイルと差分を確認する。
- 原則: 事実と前提を分け、推測を事実のように断定しない。

### 変更方針

- 原則: 既存の設計、命名、書式に沿う最小限の変更を優先する。
- 必須:
  - ユーザーの変更を保持する。
  - 無関係なファイルを変更しない。
  - 破壊的な変更の前に、対象、影響、より安全な代替案を説明する。

## プロジェクト

### 概要

- このリポジトリは Astro で構築し、GitHub Pages へ公開する静的な個人サイトである。
- 原則: サイトは軽量に保ち、UI フレームワークやサーバーサイドの依存関係を追加しない。
- 例外: 明確な必要性がある場合に限り、追加を許容する。

### 配置

- ルート: `src/pages/`
- 再利用する UI: `src/components/`

### 機密情報

- 禁止: 機密情報をコミットしない。
- 原則: 環境変数ファイルは無視し、機密でない例だけを `.env.example` に置く。

### 実装前の確認

- 必須: 実装前に次の資料を確認する。
- 対象:
  - サイト方針: `docs/product.md`
  - 開発規約: `docs/conventions.md`
  - 開発フロー: `docs/development.md`

## レビューと検証

### レビュー範囲

- 原則: ユーザーが依頼した範囲だけを対象にする。
- 対象: `git diff --cached` のレビューでは、ステージ済みの差分だけを確認する。

### レビュー観点

- 優先対象:
  - バグ
  - 回帰
  - 仕様との不整合
  - セキュリティ上の問題
  - 検証不足
- 報告:
  - 指摘を重要度順に記載する。
  - 該当ファイルと根拠を添える。
  - 必須修正と任意改善を分ける。

### 検証

- 原則: 実装を変更した場合は、変更内容に応じた検証を実行する。
- 標準コマンド:

  ```sh
  npm run format:check
  npm run lint
  npm run build
  ```

### コミット前の確認

- 必須: `git diff --cached` を確認し、無関係なファイルや機密情報がステージされていないことを確認する。

## 開発

### 開発サーバー

- 必須: バックグラウンドモードで起動する。
- 起動:

  ```sh
  astro dev --background
  ```

- 管理:
  - 停止: `astro dev stop`
  - 状態確認: `astro dev status`
  - ログ確認: `astro dev logs`

## ドキュメント

### Astro

- 参照先: https://docs.astro.build
- 確認対象:
  - ルート
  - コンポーネント
  - コンテンツコレクション
  - スタイリング
  - 多言語化
- 必須: 上記の機能や構成を追加する前に、対応する Astro のガイドを確認する。
