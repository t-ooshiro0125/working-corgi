# Working Corgi エージェントガイド

## 対話と変更

- ユーザーが別の言語を指定しない限り、日本語で応答する。
- デフォルトはレビュー・助言モードとする。ファイル、Git 状態、外部サービスを変更するのは、ユーザーが明示的に依頼した場合のみとする。
- コードに依存する質問へ回答する前に、最新の関連ファイルと差分を確認する。
- 事実と前提を分け、推測を事実のように断定しない。
- 既存の設計、命名、書式に沿う最小限の変更を優先する。
- ユーザーの変更を保持し、無関係なファイルを変更しない。
- 破壊的な変更の前に、対象、影響、より安全な代替案を説明する。

## プロジェクト

- このリポジトリは Astro で構築し、GitHub Pages へ公開する静的な個人サイトである。
- サイトは軽量に保つ。明確な必要性がない限り、UI フレームワークやサーバーサイドの依存関係を追加しない。
- ルートは `src/pages/`、再利用する UI は `src/components/` に置く。
- 機密情報をコミットしない。環境変数ファイルは無視し、機密でない例だけを `.env.example` に置く。
- 実装前に、サイト方針は `docs/product.md`、開発規約は `docs/conventions.md`、開発フローは `docs/development.md` を確認する。

## レビューと検証

- レビューは依頼された範囲だけを対象にする。`git diff --cached` のレビューでは、ステージ済みの差分だけを確認する。
- 指摘は重要度順に、該当ファイルと根拠を添えて記載する。必須修正と任意改善を分ける。
- バグ、回帰、仕様との不整合、セキュリティ上の問題、検証不足を優先して確認する。
- 実装を変更した場合は、変更内容に応じた検証を実行する。標準の確認コマンドは次のとおり。

  ```sh
  npm run format:check
  npm run lint
  npm run build
  ```

- コミット前に `git diff --cached` を確認し、無関係なファイルや機密情報がステージされていないことを確認する。

## 開発

開発サーバーを起動するときは、バックグラウンドモードを使う。

```sh
astro dev --background
```

バックグラウンドサーバーは、`astro dev stop`、`astro dev status`、`astro dev logs` で管理する。

## ドキュメント

Astro のドキュメント: https://docs.astro.build

ルート、コンポーネント、コンテンツコレクション、スタイリング、多言語化を追加する前に、対応する Astro のガイドを確認する。
